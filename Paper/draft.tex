\documentclass[12pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{setspace}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{natbib}
\usepackage{booktabs}
\usepackage{hyperref}

\title{Political Polarization and Market Reactions to Auditor-Change Disclosures}
\author{Neil Hwang\\
        \small Bronx Community College, CUNY}
\date{\today}

% Hypothesis environment
\newtheorem{hyp}{Hypothesis}
\newtheorem{proposition}{Proposition}

\begin{document}
\maketitle
\doublespacing

% ── Abstract ─────────────────────────────────────────────────────────────────

\begin{abstract}
Auditor-change disclosures are mandatory, public, and often ambiguous in their
informational content: a firm may change auditors for routine reasons or in
response to fundamental disagreement over its financial reporting, and investors
must form their own assessments of which it is. Using 678 Form~8-K Item~4.01
filings from 2001 to 2023, we examine how the political environment in which
investors are embedded shapes their interpretation of this mandatory audit-related
signal. Exploiting cross-state variation in presidential electoral competitiveness
as a proxy for investor belief heterogeneity, we find that absolute abnormal
returns are 0.47 percentage points larger and abnormal trading volume is 0.11
units higher around auditor-change disclosures in more politically competitive
states ($p = 0.040$ and $p = 0.010$, respectively). Effects are concentrated in
auditor resignations --- the more informationally ambiguous class of changes ---
consistent with polarization amplifying investor disagreement most when the
disclosure provides less inherent guidance about fault or severity. These findings
suggest that the effectiveness of mandatory audit-related disclosure as an
information transmission mechanism depends not only on what is disclosed, but on
the social and political environment in which investors interpret it.
\end{abstract}

% ── 1. Introduction ───────────────────────────────────────────────────────────

\section{Introduction}
\label{sec:intro}

Auditor-change disclosures are among the most consequential mandatory filings
in the Form~8-K reporting system. The mandatory filing (Form~8-K Item~4.01) must include the reasons for the
change and any disagreements between the firm and its outgoing auditor.
Prior research establishes that these disclosures are informative: abnormal
returns around auditor changes reflect assessments of future reporting quality,
regulatory relationships, and management credibility
\citep{dodd1984, defond1998, landsman2009}. Yet the magnitude of market reactions
varies substantially across otherwise similar events, and the sources of that
cross-sectional variation are incompletely understood. This paper identifies one
systematic and previously undocumented source: the political environment in which
investors are embedded. We show that investor interpretation of the same mandatory
audit-related disclosure varies with the degree of political division in the firm's
headquarters state, generating larger capital-market reactions where investors
are more likely to hold heterogeneous priors about audit credibility and
regulatory oversight.

Auditor-change disclosures are well-suited for studying heterogeneous investor
interpretation for three reasons. First, they are standardized and public ---
all investors receive the same filing simultaneously, via the same EDGAR channel
--- so differences in reactions across firms cannot be attributed to information
timing, access, or format differences. Second, the signal is inherently ambiguous:
a firm may change auditors for routine reasons (fee negotiation, committee rotation
policy) or for troubling ones (disagreements over accounting estimates, auditor
resignation under pressure), and the 8-K frequently provides incomplete guidance
about which. Investors must form their own assessments. Third, auditing is an
institution-dependent activity: the credibility of the signal depends on investors'
trust in auditors, the PCAOB, and the broader regulatory infrastructure that
underwrites audit quality. That trust varies systematically with political identity,
making the auditor-change setting especially exposed to politically driven
differences in interpretation.

Our theoretical framework builds on \citet{kandel1995}: investors observe the
same public audit signal but form different posteriors depending on their priors
about firm quality, auditor credibility, and the reliability of regulatory oversight.
Political polarization amplifies this heterogeneity. Polarized investors diverge
not only in their substantive interpretations of an auditor change --- whether
it signals earnings manipulation risk or routine governance --- but also in
their assessments of whether auditors and the PCAOB are credible certifiers
of financial reporting quality. This divergence is distinct from information
asymmetry: polarization does not change what is disclosed, only how investors
with different priors process it. Under standard belief-driven trading models
\citep{harris1993}, greater posterior dispersion translates into higher abnormal
volume and larger absolute price movements around the disclosure.

We identify this effect using cross-state variation in presidential electoral
competitiveness. Our primary polarization proxy is one minus the absolute
two-party presidential vote margin at the state level, constructed from MIT
Election Lab county-level returns and forward-filled to annual frequency.
We use presidential rather than congressional returns because presidential
elections are the most nationalized arena of U.S. electoral competition ---
outcomes are driven by broad national partisan forces rather than candidate-
or district-specific factors --- making them a cleaner proxy for the partisan
belief environment \citep{vertz1987, abramowitz2016, moskowitz2021}.
More competitive states --- where the presidential electorate is more evenly
divided --- serve as a proxy for investor environments with greater belief
heterogeneity. We acknowledge a key limitation of this proxy: institutional
investors are geographically dispersed and need not share the political
characteristics of the firm's headquarters state. We treat headquarters-state
competitiveness as a noisy reduced-form proxy for the investor belief environment
rather than a precise measure of individual investor beliefs; measurement error
of this kind attenuates estimated coefficients toward zero, so our findings are
likely conservative. Our sample consists of 678 Form~8-K Item~4.01 filings from
2001 to 2023, matched to CRSP daily return and volume data and Compustat firm
characteristics. Our main specification regresses $|CAR(-1,+1)|$ and abnormal
trading volume on standardized state competitiveness, controlling for firm size,
leverage, profitability, and book-to-market ratio, with year and industry
fixed effects throughout.

We find that firms headquartered in more politically competitive states experience
significantly larger absolute abnormal returns and abnormal trading volume around
auditor-change disclosures. A one-standard-deviation increase in state partisan
competitiveness is associated with a 0.47 percentage point increase in
$|CAR(-1,+1)|$ ($\beta = 0.005$, $p = 0.040$) and a 0.11-unit increase in
abnormal volume ($\beta = 0.111$, $p = 0.010$), controlling for firm
characteristics, year fixed effects, and industry fixed effects. These effects
are concentrated in auditor resignations rather than client-initiated dismissals
($\beta = 0.012$, $p = 0.043$ vs.\ $\beta = 0.003$, $p = 0.203$), consistent
with the prediction that polarization amplifies investor disagreement most when
the disclosure provides less inherent guidance about fault or severity. The
main coefficient is stable across alternative polarization proxies (DW-NOMINATE
ideological gap, presidential Esteban-Ray index) and survives two-way clustering
of standard errors by firm and state.

This paper makes three contributions. First, we provide new evidence that
capital-market reactions to mandatory auditor-change disclosures vary
systematically with the political environment in which investors are embedded.
Prior work establishes that auditor changes are informative on average
\citep{firth1978, dodd1984, defond2014}; we show that the magnitude of the
market reaction depends on how divided that investor environment is, not only
on the content of the disclosure. Second, we identify political polarization as
a determinant of investor interpretation heterogeneity in an auditing setting.
Existing work on the political economy of capital markets focuses on asset
allocation, partisan trading behavior, and analyst forecasts \citep{kempf2024};
we show that polarization also shapes how investors process mandatory audit
disclosures, a setting where institutional trust is unusually central to signal
interpretation. Third, we connect to the literature on the effectiveness of
mandatory disclosure: our evidence suggests that the informational consequences
of a required filing depend not only on its content but on the social and
political environment in which investors interpret it, a finding with implications
for how disclosure-based regulation performs across heterogeneous investor
populations.

These findings bear on the design of mandatory audit disclosure requirements.
A common motivation for expanded audit reporting --- including critical audit
matters under PCAOB AS~3101 --- is that richer disclosures reduce investor
uncertainty and improve market efficiency. Our evidence suggests a limit to
this logic: when investors differ systematically in their assessments of
institutional credibility, the same disclosure may generate more, not less,
disagreement. This is not an argument against disclosure, but it does suggest
that the effectiveness of disclosure-based regulation depends on the
interpretive environment, not only on the information provided.

The remainder of the paper proceeds as follows. Section~\ref{sec:lit} reviews
the related literature. Section~\ref{sec:theory} develops the theoretical
framework. Section~\ref{sec:hypotheses} states the testable hypotheses.
Section~\ref{sec:design} describes the empirical design. Section~\ref{sec:results}
presents the results. Section~\ref{sec:conclusion} concludes.

% ── 2. Related Literature ─────────────────────────────────────────────────────

\section{Related Literature}
\label{sec:lit}

\subsection{Audit Reports and Capital Markets}

A substantial literature documents that auditor-related disclosures convey
information to investors. Early studies show that qualified audit opinions affect
stock prices \citep{firth1978, dodd1984}. Subsequent work examines how audit
quality relates to market outcomes more broadly \citep{francis2004, defond2014}.
Research on expanded audit reporting --- including critical audit matters under
PCAOB AS 3101 --- finds mixed evidence on incremental informativeness, with some
studies finding that richer disclosures affect investor decisions
\citep{christensen2014} and others finding limited price effects
\citep{gutierrez2018}. We build on this literature by asking not whether audit
signals are informative in the aggregate, but whether their interpretation varies
systematically with the political environment.

\subsection{Auditor Changes}

Auditor changes are among the most salient audit-related events. The
direction and magnitude of market reactions depend on the nature of the
switch \citep{johnson1990, defond1998, landsman2009}: Big-4 departures tend
to generate negative abnormal returns, while Big-4 appointments are viewed
more favorably. We use these events as our primary setting because the
mandatory, standardized disclosure format makes them ideal for studying
cross-sectional differences in interpretation.

\subsection{Political Economy of Auditing}

A related literature examines how political factors influence audit outcomes.
\citet{chan2006} document that political connections shape auditor reporting
behavior and auditor switching in a cross-country setting, showing that audit
opinions respond to political incentives rather than purely economic ones.
More broadly, the political economy of auditing encompasses how regulatory,
institutional, and political pressures shape the supply of audit quality
\citep{francis2004, defond2014}. \citet{lennox2010} show that Big~4 auditor
oversight is associated with lower fraud incidence, underscoring that the
institutional infrastructure surrounding auditing---including regulatory
design and oversight---has measurable consequences for financial reporting
quality.

Our paper examines a different and largely unexplored dimension: the
\emph{investor side} of the audit signal. This mechanism is particularly
salient in the context of PCAOB oversight, whose authority and credibility
as a regulatory body may not be assessed uniformly across investor groups.
Investors who are more skeptical of regulatory institutions --- a tendency
documented to vary systematically with political identity \citep{kempf2024}
--- may discount the credibility of auditor disclosures differently from
investors who trust regulatory oversight. This mechanism is orthogonal to
the auditor-side political economy literature and complements it: even if
political pressures leave auditor behavior unchanged, polarization in the
investor population can amplify the capital-market consequences of those
disclosures.

\subsection{Political Polarization and Capital Markets}

A rapidly growing literature documents that political polarization affects
capital markets. \citet{kempf2024} survey evidence that partisan identity
influences asset allocation, trading behavior, and return expectations. Related
work finds that political beliefs shape economic forecasts and risk perceptions.
The political science literature distinguishes ideological from affective
polarization (reviewed in Section~\ref{sec:affective} below); both forms are
relevant here, because auditor disclosures are institution-dependent signals
whose interpretation may be shaped by substantive policy disagreement and by
affective distrust of gatekeepers alike. We extend this literature to the
auditing context, where institutional trust plays an unusually central role.

\subsection{Affective Polarization, Social Identity, and Information Processing}
\label{sec:affective}

The recent political science literature distinguishes ideological polarization
--- divergence in substantive issue positions --- from affective polarization
--- out-party animus, distrust, and social distance that can intensify even
when policy views are moderate \citep{iyengar2012, iyengar2019, mason2015,
druckmanlevy2021}. \citet{iyengar2019} document that partisan animus spills
over into nonpolitical settings --- employment decisions, commercial
transactions, and interpersonal trust --- even when the domain has no direct
political content. This nonpolitical spillover makes it plausible that
affective polarization also shapes how investors assess the credibility of
institution-dependent disclosures such as auditor changes.

For measurement, \citet{tyler2023} evaluate the robustness of the ANES
feeling-thermometer series as an indicator of affective polarization. They
find that the increase in out-party animus is real but that mixed survey modes
can inflate estimates in the later part of the time series. We use the ANES
series as a supplementary national proxy rather than a primary baseline,
consistent with \citet{tyler2023}'s recommendation to treat thermometer
measures as useful but not perfectly clean annual indicators.

\subsection{Heterogeneous Beliefs and Trading}

Our theoretical framework draws on models of heterogeneous beliefs in financial
markets. \citet{harris1993} show that differences of opinion generate trade even
without private information. \citet{kandel1995} demonstrate that investors with
heterogeneous priors interpret identical public signals differently, generating
trading volume and price effects. \citet{hong2007} survey the broader literature
on disagreement and asset prices. We contribute to this literature by linking
belief heterogeneity to a specific, measurable source --- political polarization
--- and by identifying the audit disclosure setting as a clean empirical context.

% ── 3. Theory ─────────────────────────────────────────────────────────────────

\section{Theory}
\label{sec:theory}

We formalize polarization as a latent force that increases heterogeneity in
investors' interpretation of auditor-originated disclosures. The Bayesian model
that follows operationalizes this heterogeneity through prior beliefs, which is
the most tractable formal channel. Institutional trust and perceived signal
precision should be understood as reduced-form sources of prior-like
heterogeneity: a less trusting investor effectively places a lower prior on the
informativeness of the auditor's signal; an investor who perceives lower signal
precision effectively flattens the likelihood function. Either way, the
formal result is the same increase in posterior dispersion. The model therefore provides a microfoundation for the broader mechanism
described in the introduction, not a claim that priors are the only operative
channel. In the affective-polarization literature \citep{iyengar2012,
iyengar2019}, partisan identity and out-party distrust shape how individuals
evaluate the credibility of information and the institutions that produce it.
In the present setting, these forces can be interpreted as widening the
cross-investor gap in trust in the disclosure ($\tau_i$) or in perceived
signal precision ($\kappa_i$). As shown in Appendix~\ref{app:proofs}, all
three channels generate the same posterior-dispersion comparative static:
whenever polarization widens the cross-group posterior log-odds index, belief
dispersion and disagreement-sensitive market outcomes strictly increase. The
heterogeneous-priors model below should therefore be read as a tractable
benchmark case of this broader class of polarization mechanisms.
The model draws on the heterogeneous-priors framework of \citet{kandel1995}.

\subsection{Setting}

Consider a firm whose true quality $\theta \in \{0, 1\}$ is unknown to
investors. The firm discloses an auditor-related signal $s \in \{0, 1\}$ (e.g.,
an auditor change announcement). Signals are informative: $P(s=1 \mid
\theta=1) = p_H > P(s=1 \mid \theta=0) = p_L$, with $p_H > p_L$.

A continuum of investors $i \in [0,1]$ observe $s$ and form posterior beliefs.
Investors share the same likelihood function $\{p_H, p_L\}$ but differ in their
prior beliefs $\pi_i \equiv P_i(\theta = 1) \in (0,1)$.

\textbf{Source of prior heterogeneity.} We model investors as belonging to
ideological groups indexed by political affiliation $g \in \{D, R\}$, with
population shares $s_D$ and $s_R = 1 - s_D$. Group-level priors reflect
differential trust in auditors and regulatory institutions: $\pi_D \neq \pi_R$.
In the model, all heterogeneity is captured by differences in $\pi_i$; in
practice, polarization may also affect how investors perceive signal precision
($p_H, p_L$) or how much weight they place on the disclosure relative to their
priors. The reduced-form predictions are the same: greater political distance
generates greater dispersion in posterior beliefs.
Polarization increases the \emph{dispersion} of priors across groups without
changing the \emph{average} prior.

\subsection{Belief Updating}

Investor $i$'s posterior belief after observing $s=1$ is:
\begin{equation}
    \label{eq:bayes}
    P_i(\theta = 1 \mid s = 1)
    = \frac{p_H \, \pi_i}{p_H \, \pi_i + p_L (1 - \pi_i)}
    \equiv \phi(\pi_i).
\end{equation}
The function $\phi(\cdot)$ is strictly increasing and concave in $\pi_i$.

We now establish the central comparative static of the model: that any
widening of the gap between group priors --- holding the average prior
fixed --- increases the dispersion of posterior beliefs. This is the
formal sense in which polarization amplifies disagreement.

\begin{proposition}[Polarization and belief dispersion]
    \label{prop:dispersion}
    Fix population shares $s_D, s_R > 0$ with $s_D + s_R = 1$ and mean prior
    $\bar{\pi} = s_D \pi_D + s_R \pi_R$. The variance of posterior beliefs
    across investor groups is
    \[
        \mathrm{Var}_i[\phi(\pi_i)]
        = s_D s_R \bigl(\phi(\pi_D) - \phi(\pi_R)\bigr)^2.
    \]
    For any increase in $|\pi_D - \pi_R|$ that holds $\bar{\pi}$ fixed,
    $\mathrm{Var}_i[\phi(\pi_i)]$ strictly increases.
\end{proposition}

\noindent\textit{Proof.} See Appendix~\ref{app:proofs}. $\square$

\medskip
\noindent\textbf{Remark.} Proposition~\ref{prop:dispersion} isolates the
mechanism through which polarization enters the model. The key condition is
a \emph{mean-preserving spread} of priors: polarization shifts the two groups
farther apart while leaving the population-average prior $\bar\pi$ unchanged.
This rules out the alternative that polarization merely correlates with
aggregate pessimism or optimism about firm quality. Under this condition, the
posterior variance increases strictly --- more polarized priors generate more
dispersed beliefs after observing the same signal. The proposition thereby
provides the microfoundation for both empirical predictions: more dispersed
posteriors produce higher disagreement-driven trading volume (Prediction~1)
and, through demand imbalance, larger absolute price movements (Prediction~2).
The result is stated for a tractable two-group setting; extension to
richer prior distributions would require additional assumptions on the
shape of the distribution and is left for future work.

\subsection{Equilibrium Prices and Trading}

Following \citet{harris1993}, prices aggregate beliefs:
\begin{equation}
    \label{eq:price}
    P^* = \int_0^1 w_i \, \phi(\pi_i) \, di,
\end{equation}
where $w_i$ are equilibrium portfolio weights. Trading volume is increasing in
posterior belief dispersion \citep{kandel1995}:
\begin{equation}
    \label{eq:volume}
    V \text{ increasing in } \mathrm{Var}_i[\phi(\pi_i)].
\end{equation}

The equilibrium price $P^*$ is a weighted average of investors' posterior
beliefs. By Proposition~\ref{prop:dispersion}, an increase in polarization
that constitutes a mean-preserving spread of priors leaves $P^*$ unchanged
in expectation but increases the cross-sectional dispersion of posteriors.
This has two distinct empirical implications that motivate our outcome
variables.

First, when some investors revise beliefs strongly upward and others strongly
downward in response to the same signal, the absolute price movement
$|P^* - P_0|$ is increasing in posterior dispersion even when the average
direction is zero. This maps directly to our first empirical outcome, the
absolute cumulative abnormal return $|CAR[-1,+1]|$: a more polarized
investor base produces larger absolute price reactions to the same
auditor-change disclosure without necessarily producing a predictable
direction. In this sense the model predicts cross-sectional variation in
\emph{how much} prices move, not \emph{which way} they move.

Second, trade is generated by the meeting of investors who update positively
with those who update negatively. Equation~\eqref{eq:volume} formalizes the
\citet{kandel1995} result that disagreement-driven turnover is proportional
to the variance of posterior beliefs: more dispersed posteriors mean more
investor pairs with mutually beneficial trades, and hence higher aggregate
volume. This maps to our second outcome variable, abnormal trading volume
($AbVol$), measured as the mean log-volume deviation from the estimation-window
mean over the $[-1,+1]$ event window. Combining equations \eqref{eq:price} and \eqref{eq:volume} with
Proposition~\ref{prop:dispersion}, the model predicts that both $|CAR|$ and
$AbVol$ are increasing in political polarization. The prediction for volume
is especially clean: unlike price, which depends on equilibrium weights
$w_i$ that can in principle offset belief dispersion, volume rises
monotonically with the variance of posteriors under standard no-short-selling
or symmetric-trading assumptions \citep{harris1993}.

\subsection{Role of Signal Ambiguity}
\label{sec:ambiguity}

The Bayesian framework also generates a result with respect to signal
informativeness that is central to our empirical design. Differentiating
the posterior with respect to the prior:
\begin{equation}
    \label{eq:sensitivity}
    \phi'(\pi_i) = \frac{p_H \, p_L}{\bigl[p_H \pi_i + p_L(1-\pi_i)\bigr]^2}.
\end{equation}
This expression makes the role of signal informativeness transparent through
two limiting cases. In the extreme case of a fully revealing signal ($p_H = 1,
p_L = 0$), $\phi'(\pi_i) = 0$ for all $\pi_i$: every investor updates to the
same posterior regardless of prior, so polarization has no effect on posterior
dispersion. In the opposite extreme of an uninformative signal ($p_H = p_L$),
$\phi(\pi_i) = \pi_i$ and posteriors equal priors, so prior heterogeneity maps
directly into posterior dispersion. Between these extremes, less informative
signals leave posteriors closer to priors and therefore allow prior
heterogeneity --- and hence polarization --- to contribute more to posterior
dispersion. This motivates the prediction that the polarization effect is
larger for more ambiguous auditor-change events.

In our setting, signal ambiguity varies systematically across auditor-change
events. An auditor departure following a disclosed disagreement over accounting
estimates or a qualified opinion is more informationally salient---and therefore
less ambiguous---than a routine rotation. A switch from a non-Big-4 to a Big-4
auditor carries a clear quality connotation; a Big-4-to-Big-4 lateral move is
harder to evaluate. Events with no stated reason for the change, or where the
disclosed rationale is generic (e.g., ``to reduce costs''), leave investors
with greater interpretive latitude. We construct a \texttt{high\_ambiguity}
indicator from observable filing characteristics and interact it with the
polarization measure in our main specifications (Section~\ref{sec:design}).
The ambiguity amplification prediction is a testable restriction of the model:
it requires not just a polarization main effect but a
polarization\,$\times$\,ambiguity interaction with positive sign.

\subsection{Testable Predictions}

\begin{itemize}
    \item[\textbf{P1.}] \textit{Polarization and trading volume (primary formal
    prediction).} Equation~\eqref{eq:volume} and the \citet{kandel1995}
    mechanism predict that disagreement-driven turnover is increasing in
    posterior belief variance. Greater polarization generates more investor
    pairs with divergent posteriors and hence mutually beneficial trading
    opportunities, raising aggregate volume even in the absence of private
    information. This prediction follows directly from
    Proposition~\ref{prop:dispersion}: polarization increases posterior
    variance, which increases $V$. We test P1 using abnormal trading volume
    ($AbVol$) as the outcome (defined in Section~\ref{sec:design}).

    \item[\textbf{P2.}] \textit{Polarization and absolute returns (secondary
    empirical prediction).} When investors on opposing sides of the prior
    distribution revise beliefs in opposite directions, absolute price
    movements are amplified even when the average signed reaction is close to
    zero. This prediction is consistent with the model's dispersion mechanism,
    but the link from posterior variance to $|CAR|$ depends additionally on
    demand asymmetry, short-sale constraints, or limits to arbitrage that are
    not explicitly modeled here \citep{hong2007}. We therefore treat $|CAR|$
    as a complementary empirical outcome: evidence that polarization increases
    both $AbVol$ and $|CAR|$ is more informative than either alone, but $AbVol$
    is the cleaner formal prediction. We test P2 by regressing $|CAR[-1,+1]|$
    on the state-year polarization measure, with the same controls and fixed
    effects as P1.

    \item[\textbf{P3.}] \textit{Ambiguity amplification.} The signal-sensitivity
    result (Section~\ref{sec:ambiguity}) predicts that polarization effects
    are larger when audit signals are more ambiguous---that is, when $p_H -
    p_L$ is smaller. We operationalize this as an interaction between the
    polarization measure and a \texttt{high\_ambiguity} indicator constructed
    from observable filing characteristics. The prediction requires a positive
    and significant interaction coefficient, above and beyond any polarization
    main effect. This test is important because it distinguishes the
    \emph{interpretation channel} from a simpler \emph{salience} story: if
    polarization merely amplified attention to any salient news, the
    polarization effect would not be systematically larger for inherently
    ambiguous events.
\end{itemize}

\textbf{Discussion.} The model formalizes one specific channel---heterogeneous
priors---but the same comparative statics arise under the other two channels
described in the introduction: heterogeneous institutional trust and
heterogeneous perceived signal precision. Both can be nested in a unified
framework. Let investor $i$'s posterior log-odds index be
\[
    X_i = a(\pi_i) + \tau_i \kappa_i \ell,
\]
where $a(\pi_i) = \log[\pi_i/(1-\pi_i)]$ is the prior log-odds, $\tau_i \in
[0,1]$ is investor $i$'s trust in the disclosure, $\kappa_i > 0$ is perceived
signal precision, and $\ell = \log(p_H/p_L) > 0$ is the objective
log-likelihood ratio. Posterior beliefs are $\mu_i = \sigma(X_i)$, where
$\sigma(\cdot)$ is the logistic map. Whenever polarization widens the
cross-group gap $|X_D - X_R|$---whether through divergent priors, divergent
trust, or divergent perceived precision---the logistic map is strictly
increasing, so $|\mu_D - \mu_R|$ increases strictly. It follows that
posterior variance $s_D s_R (\mu_D - \mu_R)^2$ increases under any channel
(see Appendix~\ref{app:proofs}). The core prediction is therefore
channel-agnostic: polarization amplifies disagreement-sensitive market
outcomes regardless of which specific mechanism predominates. The ambiguity
interaction in P3 is similarly robust: ambiguous signals reduce $\ell$,
which increases the relative weight of the non-signal components ($a(\pi_i)$
and $\tau_i$), so prior and trust heterogeneity contribute more to posterior
dispersion. A further implication is that standard disclosure-based remedies
cannot fully eliminate polarization-driven disagreement, because the
heterogeneity lies in how investors process signals, not in what the signals
contain.

% ── 4. Hypothesis Development ─────────────────────────────────────────────────

\section{Hypothesis Development}
\label{sec:hypotheses}

\begin{hyp}
    \label{h1}
    Investor reactions to auditor change disclosures are more extreme in
    politically polarized environments. Specifically, both the absolute
    cumulative abnormal return ($|CAR|$) and abnormal trading volume around
    auditor change events are increasing in political polarization.
\end{hyp}

\noindent\textbf{Exploratory Prediction (Event Type).}
The formal model does not explicitly parameterize event stakes, so this
prediction is not directly derived from the theoretical framework. We
nonetheless examine whether the polarization effect is stronger for
higher-stakes auditor changes --- specifically, auditor dismissals.
Dismissals are initiated by the client, often amid concerns about financial
reporting or accounting judgments, making them more informationally
consequential than routine resignations. Higher stakes amplify whatever
interpretation investors form: more is riding on the outcome, so
polarization-induced divergence in interpretation translates into larger
disagreement and more extreme reactions. This is a salience and stakes
conjecture, not an ambiguity prediction --- dismissals need not be more
ambiguous than resignations to generate stronger polarization effects; they
need only be more consequential. We treat this as an auxiliary empirical
exercise (Table~\ref{tab:hetero}) rather than a core hypothesis.

\begin{hyp}
    \label{h2}
    The polarization effect on abnormal trading volume and absolute returns is
    stronger for auditor changes that carry more ambiguous signals --- events
    where the informativeness of the disclosure ($p_H - p_L$) is lower.
    Per Section~\ref{sec:ambiguity}, ambiguity amplifies the role of prior
    heterogeneity in determining posterior dispersion. This hypothesis is
    formally derived from the model and is the primary cross-sectional test
    of the interpretation channel. It is distinct from the exploratory
    event-type prediction above: that prediction concerns the stakes of the
    event; this hypothesis concerns how much interpretive latitude the
    disclosure leaves investors.
\end{hyp}

% ── 5. Empirical Design ───────────────────────────────────────────────────────

\section{Empirical Design}
\label{sec:design}

\subsection{Sample Construction}

\textbf{Auditor change events.} We identify auditor changes using Form 8-K
filings disclosing a change in the registrant's certifying accountant (Item
4.01), obtained from SEC EDGAR for 2001--2023.\footnote{We end the sample in
2023, the last full year for which DW-NOMINATE ideological scores are
available in final form from Voteview. Scores for the 119th Congress (which
began January 2025) have not yet been finalized at the time of writing. The
Esteban-Ray electoral measure covers the same period, as MIT Election Lab
House returns are available through the 2022 midterm election, which is
forward-filled to cover calendar years 2023 and 2024.} We retain only
original 8-K filings matched to a CRSP PERMNO and require at least 100
non-missing trading days in the estimation window.\footnote{Of the 8,621
auditor-change events identified from EDGAR, 1,720 are 8-K/A amendments and
are excluded, leaving 6,901 original filings. Of these, 5,641 cannot be
matched to a CRSP/Compustat security and are dropped, and a further 40 are
excluded for insufficient trading-day coverage in the estimation window.
The unmatched filings are predominantly firms not listed on major exchanges
or not covered by CRSP/Compustat.} The final sample consists
of 678 auditor change events for 456 unique firms. We exclude financial firms
(SIC 6000--6999) and utilities (SIC 4900--4999).

\subsection{Dependent Variables}

\textbf{Absolute cumulative abnormal return ($|CAR|$).} Following standard
event-study methodology \citep{mackinlay1997}, we estimate a market model
over the estimation window $[-252, -46]$ trading days relative to the event
date, using at least 100 non-missing trading days to estimate parameters:
\begin{equation}
    AR_{e,t} = R_{e,t} - (\hat{\alpha}_e + \hat{\beta}_e \cdot R_{m,t}),
    \quad t \in \{-1, 0, +1\}.
\end{equation}
The window begins 252 days before the event to capture a full trading year
of pre-event returns and ends 46 days before to avoid contamination from
information leakage in the run-up period. $CAR_e = \sum_{t=-1}^{+1} AR_{e,t}$
and $|CAR_e|$ is the primary dependent variable. We use the CRSP
value-weighted return as the market return and include delisting returns
following \citet{beaver2007}.

\textbf{Abnormal trading volume ($AbVol$).} We measure trading-volume
reactions over the $[-1, +1]$ event window, consistent with the short-run
agreement-to-disagree framework of \citet{kandel1995} in which volume spikes
at the moment beliefs diverge. $AbVol_e$ is the mean of
$[\log(\mathrm{Vol}_{e,t}+1) - \overline{\log(\mathrm{Vol}_e)}]$ over the
$[-1,+1]$ window, where the baseline is the estimation-window mean.

\subsection{Political Polarization Measure}

A central empirical challenge is that the theory's polarization object ---
the latent force that increases divergence in interpretation through identity,
alienation, and perceived institutional credibility --- is not directly
observable at the investor level \citep{duclos2004}. The available data
provide only grouped or indirect proxies, each of which captures a different
margin of the latent concept \citep{esteban2007}. We therefore use three
complementary proxies rather than treating any single measure as definitive.
The state-level presidential competitiveness measure below captures
\emph{cross-sectional partisan division}; DW-NOMINATE captures
\emph{elite ideological distance}; and the ANES affective series captures
\emph{national partisan animus}. These are not three ways of measuring the
same thing; they triangulate a latent object from distinct angles
\citep{wang2000}.\footnote{%
The continuous-density estimators developed by \citet{duclos2004} provide
the most rigorous formalization of the identity--alienation concept, but
require a directly observed distribution of the underlying investor-level
trait. Because our setting does not yield a firm-year density of investor
political identity, we use reduced-form political-environment proxies instead.}

We measure state-level partisan competitiveness from county-level presidential
election returns (MIT Election Lab, 2000--2020). For state $s$ and presidential
election year $e$, let $D_{s,e}$ and $R_{s,e} = 1 - D_{s,e}$ denote the
Democratic and Republican two-party vote shares aggregated across counties.
The partisan margin is $M_{s,e} = |D_{s,e} - R_{s,e}|$, ranging from zero in
perfectly contested states to one in completely one-sided states. We define
\emph{partisan competitiveness} as:
\begin{equation}
    \label{eq:comp}
    \text{Competitiveness}_{s,e} = 1 - M_{s,e} = 1 - |D_{s,e} - R_{s,e}|,
\end{equation}
which is maximized at 1.0 when both parties split the vote equally and
minimized at 0 in one-party states. More competitive states are politically
divided environments where investors are more likely to hold heterogeneous priors
about institutional trustworthiness and the interpretation of regulatory
signals.\footnote{We also test the Esteban-Ray polarization index
$P^{ER}_{s,e} = D_{s,e} \cdot R_{s,e}$, computed at the state level from the
same presidential returns. The ER index is concave in vote shares and
has lower discriminating power away from the 50-50 split (SD in our sample:
0.027 for the ER index vs.\ 0.101 for the margin). We therefore prefer the
linear competitiveness measure as the primary proxy and retain the presidential
ER index as a robustness check (Table~\ref{tab:robust}, column 2). The
congressional-district ER index based on U.S. House returns is also tested in
column 3 of Table~\ref{tab:robust}; its standard deviation is only 0.011 ---
one-ninth of the presidential margin --- reflecting gerrymandering and the
compression of competitive outcomes at the district level. \citet{mehlhaff2024group}
shows that the ER index at $\alpha=1$ is nearly indistinguishable from the
variance of the two-party vote distribution (correlation $\approx 0.93$); the
margin is a simpler and more transparent linear proxy. We prefer presidential
over congressional returns for a conceptual reason as well: a well-established
body of political-science research documents that presidential races are the
most nationalized arena of U.S. electoral competition, in the sense that
national partisan forces determine outcomes cleanly with minimal candidate- or
district-specific noise \citep{vertz1987, carson2024}. Congressional and
subnational races, by contrast, absorb additional local information and
candidate-specific variation that dilutes the national partisan signal
\citep{moskowitz2021, abramowitz2016, conevska2025}. Using presidential
returns as the basis for our competitiveness measure therefore better isolates
the broad, national-level partisan cleavage that we hypothesize shapes investor
belief heterogeneity.}

We forward-fill each presidential election's measure over the four subsequent
calendar years --- assigning the 2000 vote to 2001--2004, the 2004 vote to
2005--2008, and so on, through the 2020 election covering 2021--2024 --- on the
assumption that the state political environment evolves at the electoral-cycle
frequency rather than annually. We standardize to mean zero and unit variance
within the analysis sample. Because different polarization indices emphasize
different aspects of between-group separation and within-group cohesion, the
competitiveness measure cannot be treated as exhaustive \citep{wang2000}.
A complementary measure based on elite roll-call behavior is therefore needed.

As a second proxy for polarization, we construct a measure of elite ideological
distance from DW-NOMINATE first-dimension scores \citep{poole1997,poole2007}.
DW-NOMINATE scores capture legislators' revealed ideological positions from
roll-call voting records, rather than electoral outcomes, and thus measure
\emph{ideological} polarization directly. For each state $s$ and Congress $c$,
we compute:
\begin{equation}
    \label{eq:dw}
    P^{DW}_{s,c} = \overline{\text{dim1}}_{R,s,c}
    - \overline{\text{dim1}}_{D,s,c},
\end{equation}
where $\overline{\text{dim1}}_{R,s,c}$ and $\overline{\text{dim1}}_{D,s,c}$
are the mean first-dimension DW-NOMINATE scores among House Republicans and
Democrats from state $s$ in Congress $c$, respectively. Because the first
dimension captures the liberal--conservative continuum, $P^{DW}_{s,c}$ is
always positive and increasing in ideological distance between the parties.
We expand each Congress's scores to the two calendar years it covers and
standardize to mean zero and unit variance. The national-level counterpart,
$P^{DW\text{-nat}}_{c}$, computes the same gap across all House members
nationally and varies only over time, providing a pure time-series check.
The competitiveness and DW-NOMINATE measures are complementary proxies:
competitiveness captures electoral division in the mass electorate, while
DW-NOMINATE captures ideological distance among elected representatives.
If both predict the same investor response, the evidence for a polarization
mechanism is substantially stronger.

\smallskip
\noindent\textit{Limitations of measurement.} No single empirical measure
fully captures the latent identity--alienation object \citep{duclos2004}.
The presidential competitiveness proxy, DW-NOMINATE ideological gap, and ANES
affective series each capture a different margin of the latent concept:
mass-electorate partisan division, elite ideological distance, and national
partisan animus, respectively. Because these proxies differ in data source, geographic
aggregation, and conceptual emphasis, convergent evidence across all three is
more informative than consistency within any single measure. Divergence across
proxies would instead indicate that the estimated effect is specific to one
dimension of polarization, a finding that would itself be informative about
the operative channel.

\subsection{Supplementary Affective Polarization Measure}

The identity--alienation framework of \citet{duclos2004} suggests that
issue distance is only one component of polarization. Affective antagonism
--- partisan animus, out-group distrust, and social distance --- is
conceptually distinct from ideological divergence and is especially relevant
for a mechanism operating through institutional trust. Both the ER and
DW-NOMINATE measures primarily capture ideological polarization; neither
directly measures how much partisans distrust each other or the institutions
the opposing side tends to support. We therefore supplement these with a
national affective polarization series. This series does not replace the
ideological proxies but tests whether the paper's finding is also present
when polarization is measured through partisan animus \citep{druckmanlevy2021,
mason2015}.

We construct the affective polarization series from the ANES Time Series
Cumulative Data File. The ANES asks respondents to rate political parties on
a 0--100 feeling-thermometer scale, where higher values indicate warmer
feelings. Let $T^D_i$ and $T^R_i$ denote respondent $i$'s feeling-thermometer
rating of the Democratic and Republican parties, respectively. In each survey
year with available data (2000, 2004, 2008, 2012, 2016, 2020), we compute:
\begin{equation}
    \label{eq:ap}
    AP_t = \frac{1}{2}\left[
        \overline{\Delta_D}_t + \overline{\Delta_R}_t
    \right],
\end{equation}
where $\overline{\Delta_D}_t = \mathbb{E}_t[T^D_i - T^R_i \mid
\text{Democratic partisan}]$ is the mean in-party minus out-party
feeling-thermometer gap among Democratic identifiers, and
$\overline{\Delta_R}_t = \mathbb{E}_t[T^R_i - T^D_i \mid
\text{Republican partisan}]$ is the analogous gap among Republican
identifiers \citep{iyengar2012}. $AP_t$ is thus the average partisan
in-group favoritism across both parties. We linearly interpolate between
survey years to produce an annual series.\footnote{\citet{tyler2023} evaluate
the robustness of ANES feeling-thermometer indicators and find that the
upward trend in out-party animus is real but that mode shifts in later survey
waves (telephone to online) can inflate the time series. We treat $AP_t$ as a
supplementary national proxy rather than a clean annual panel, consistent with
their recommendation. Our supplementary specification interacts $AP_t$ with a
cross-sectional exposure variable, so any additive time-series bias from mode
changes is partially absorbed by year fixed effects in the main ideological
specifications.}

Because $AP_t$ is national, it is absorbed by year fixed effects when entered
alone. We therefore identify its role through interaction with a
time-invariant state-level \emph{exposure} variable: the long-run average
absolute Republican presidential vote margin,
$\text{Exposure}_s = \overline{|R\text{-share}_{s,t} - 0.5|}$, averaged
over all presidential elections from 2000 to 2020. High-exposure states
(e.g., Wyoming, Massachusetts) consistently vote lopsidedly; low-exposure
states (e.g., Pennsylvania, Wisconsin) are genuine swing states. The
identifying variation is thus $AP_t \times \text{Exposure}_s$: the
differential response of firms in high-partisan-intensity states as national
affective polarization rises over time. We present this as a supplementary
validation in Table~\ref{tab:affective}, not as a coequal baseline
specification, because the national AP series cannot be assigned to states
without the exposure interaction.

\subsection{Empirical Specification}

The unit of observation is an auditor change event $e$. We estimate:
\begin{equation}
    \label{eq:main}
    Y_e = \alpha + \beta\,\textit{Pol}_{s(e),\,t(e)}
    + \gamma' X_{e,\,t(e)-1}
    + \tau_{t(e)} + \delta_{j(e)} + \varepsilon_e,
\end{equation}
where $Y_e$ is $|CAR_e|$ or $AbVol_e$; $\textit{Pol}_{s(e),t(e)}$ is the
standardized state political polarization measure for the headquarters state
$s(e)$ of the event's firm in event year $t(e)$ --- a reduced-form proxy
for the polarization of the political environment in which the disclosure is
received, not a direct measure of investor political identity;
$X_{e,t(e)-1}$ is a vector of firm controls measured in the fiscal year prior
to the event (size, leverage, ROA, book-to-market, loss indicator, and sales
growth); $\tau_{t(e)}$ are event-year fixed effects; and $\delta_{j(e)}$ are
2-digit SIC industry fixed effects. Standard errors are clustered at the firm
level. The coefficient of interest is $\beta$.

\subsection{Identification}

The paper identifies whether auditor-change disclosures are interpreted
differently in more polarized political environments --- not whether any
specific type of investor trades more. Because investor-level political
identity is unobserved, the empirical design uses the headquarters-state
political environment as a reduced-form proxy for the polarization of the
context in which the disclosure is received.

\subsubsection*{Why headquarters-state polarization is informative}

The intended channel runs through the local stakeholder environment
surrounding the firm: employees, customers, local media, and --- critically
--- retail investors, who are more geographically concentrated than
institutional investors and more likely to anchor their beliefs to local
political norms. For smaller and less nationally prominent firms, the
marginal trader is more likely to be a local retail investor whose
interpretation of an auditor change is shaped by the surrounding political
environment. For larger firms with globally dispersed institutional ownership,
the headquarters-state proxy is less well-matched to the actual investor base,
and the effect should be attenuated. This cross-sectional prediction provides
a validation test: we expect the polarization effect to be stronger for
smaller firms and firms with higher retail-investor ownership, and weaker for
firms with predominantly institutional or geographically dispersed ownership
(Table~\ref{tab:robust}).

We acknowledge the main limitation of the proxy directly: equity investors in
publicly traded firms are not confined to the headquarters state. Institutional
ownership is national, and many retail investors hold diversified portfolios
regardless of geography. Headquarters-state polarization therefore captures
the intended channel with error. We address this in four ways. First, we
include rich firm-level controls to absorb observable firm characteristics
that may correlate with both state polarization and event-window reactions.
Second, state fixed effects (Table~\ref{tab:robust}, column 6) absorb all
time-invariant cross-state differences in investor clientele composition,
local media environments, state regulatory environments, and industry
clustering, identifying solely from within-state changes in polarization
across election cycles. Third, the DW-NOMINATE and ANES affective
specifications use entirely different data sources and produce variation that
is not driven by the headquarters-state aggregation choice. Convergent
findings across all three proxies would indicate that the result is not an
artifact of the ER state-level aggregation. Fourth, the county-level
polarization robustness check (online appendix) provides a finer-grained
alternative using MIT Election Lab county presidential returns matched to
firm headquarters counties via Compustat zip codes.\footnote{County-level
matching sharpens geographic identification but has its own limitations:
county presidential returns are available only quadrennially, and
congressional district boundaries do not align with county boundaries.}

\subsubsection*{State-level confounds and inference}

Identification rests on two sources of variation in $\textit{Pol}_{s(e),t(e)}$:
cross-state differences in the partisan environment of congressional elections,
and time-series changes in that environment across election cycles. Year
fixed effects absorb aggregate macroeconomic and political conditions common
to all firms. Industry fixed effects absorb sector-level differences in
auditor change frequency and investor base composition. The key identifying
assumption is that, conditional on these controls, a firm's headquarters-state
polarization at the time of the event is uncorrelated with unobserved
determinants of the market's reaction to that specific auditor change.

Four state-level confounds deserve explicit discussion. \emph{Local investor
clientele}: politically polarized states may attract systematically different
retail-investor populations whose risk tolerance or information processing
differs for reasons unrelated to polarization per se. We address this through
state fixed effects, which absorb the time-invariant component of clientele
composition. \emph{Local media ecosystems}: more polarized states may have
more polarized local media that amplifies any corporate news event, not just
auditor changes. If so, the polarization effect should appear equally for
non-audit events; the Item~5.02 placebo design (executive departures) tests
this directly. \emph{State business and regulatory environments}: states with
more polarized electorates may also have different securities litigation
climates or governance norms that independently affect how auditor changes
are processed. State fixed effects absorb time-invariant components; the
time-varying component is plausibly orthogonal to specific auditor-change
timing within the state. \emph{Industry clustering}: some high-polarization
states (e.g., Texas for energy, California for technology) have distinctive
industry concentrations. Two-digit SIC industry fixed effects absorb this
directly.

Because the polarization regressor varies at the state$\times$year level,
columns (7) and (8) of Table~\ref{tab:robust} address this concern: column (7)
clusters at the state level and column (8) implements two-way clustering by
firm and state following \citet{cameron2011}; the baseline clusters at the
firm level, which is conservative for event-level inference but potentially
optimistic relative to the regressor's level of variation.

% ── 6. Results ────────────────────────────────────────────────────────────────

\section{Results}
\label{sec:results}

\subsection{Descriptive Statistics}

Table~\ref{tab:summary} presents descriptive statistics for the 678 auditor-change
events in the estimation sample. The mean $|CAR(-1,+1)|$ is 5.0\% with a standard
deviation of 6.7\%, consistent with prior event studies of auditor changes
\citep{defond2014}. Abnormal trading volume averages 0.11 units above normal,
reflecting the information content of the disclosures. The mean partisan margin ($|D-R|$, the inverse of the primary competitiveness
measure) is 0.13 (SD 0.10), spanning from near-zero for highly competitive
states to 0.40 for strongly one-party states; equivalently, mean competitiveness
is 0.87. The median firm has log assets of 4.97 and leverage
of 17\%; 45\% report a loss in the year preceding the auditor change, reflecting
the elevated distress common in this sample.

\input{../Output/Tables/tab01_summary_stats}

\subsection{Main Results}

Table~\ref{tab:main} reports the baseline results. Columns (1)--(2) use $|CAR(-1,+1)|$
as the dependent variable; columns (3)--(4) use abnormal volume. Odd columns include
year fixed effects only; even columns add industry fixed effects and the full set of
firm controls. Across all four specifications, the coefficient on \textit{Competitiveness}
is positive and statistically significant, ranging from $\beta = 0.005$ ($p = 0.016$)
in column (1) to $\beta = 0.005$ ($p = 0.040$) in column (2) for $|CAR|$, and
$\beta = 0.118$ ($p = 0.006$) to $\beta = 0.111$ ($p = 0.010$) for abnormal volume.
The economic magnitude is meaningful: a one-standard-deviation increase in partisan
competitiveness is associated with a 0.47 percentage point increase in $|CAR|$ relative
to a sample mean of 5.0\%, and a 0.11-unit increase in abnormal volume. The consistency
of the coefficient across specifications with and without firm controls suggests that
political geography is not merely proxying for firm characteristics.

\input{../Output/Tables/tab02_main_results}

\subsection{Heterogeneity by Event Type}

Table~\ref{tab:event_type} presents split-sample regressions by auditor change type.
The effect is concentrated in non-dismissals (auditor resignations): $\beta = 0.012$,
$p = 0.043$. Among dismissals --- where the client terminates the auditor and the
adversarial nature of the separation is more transparent --- the coefficient is
$\beta = 0.003$ and statistically indistinguishable from zero ($p = 0.203$). This
asymmetry is consistent with the mechanism: when the signal is inherently ambiguous
about fault (resignation), politically heterogeneous investor bases disagree more
about its interpretation, generating larger price and volume responses. Splits by
quality direction (Big-4 departures and non-Big-4 upgrades) are also reported but
are individually insignificant, likely due to small subsamples.

\input{../Output/Tables/tab03_event_type}

\subsection{Signal Ambiguity}

Table~\ref{tab:ambiguity} splits the sample into high- and low-ambiguity events.
High-ambiguity events are those with no formally disclosed disagreement and no clear
quality change (lateral auditor-to-auditor switches or changes classified as unknown),
comprising 120 of the 678 sample observations. The $|CAR|$ coefficient in the
high-ambiguity subsample ($\beta = 0.020$, $p = 0.110$) is four times the full-sample
estimate, but does not reach conventional significance at $N = 120$; the low-ambiguity
coefficient ($\beta = 0.003$, $p = 0.209$) is smaller and also insignificant. For
abnormal volume, the pattern reverses: the low-ambiguity subsample drives the result
($\beta = 0.114$, $p = 0.008$), while the high-ambiguity volume coefficient is
imprecise ($p = 0.422$). We interpret the volume finding as evidence that events
carrying clear quality signals (disclosed disagreements, auditor quality changes)
generate more polarization-driven heterogeneous trading in competitive states, where
investors with opposing political priors disagree about the severity of the event.
The $|CAR|$ pattern is directionally consistent with the ambiguity hypothesis but
remains underpowered given the small high-ambiguity subsample.

\input{../Output/Tables/tab04_ambiguity}

\subsection{Robustness and Measurement Triangulation}

Table~\ref{tab:robust} serves a dual purpose: it addresses conventional
robustness concerns and provides measurement triangulation across the three
conceptually distinct polarization proxies described in
Section~\ref{sec:design}. No single proxy fully captures the latent
identity--alienation object \citep{duclos2004}; convergent evidence across
proxies that differ in their data source, construction, and conceptual
emphasis is therefore more informative than stability within a single measure.

Table~\ref{tab:robust} presents seven robustness columns. Column (1) repeats the
baseline for reference ($\beta = 0.005$, $p = 0.040$). Column (2) substitutes the
quadratic presidential Esteban-Ray index for the linear competitiveness measure;
the coefficient is directionally consistent but attenuated ($\beta = 0.003$,
$p = 0.124$), reflecting the lower discriminating power of the quadratic form in
non-competitive states. Column (3) uses the House-district ER index as an alternative
geographic scale; the coefficient is near zero ($\beta = 0.000$, $p = 0.893$),
consistent with the compressed cross-sectional variation (SD = 0.011) of the
House-district measure relative to state presidential margins (SD = 0.101).
Columns (4)--(5) replace the electoral measure with DW-NOMINATE roll-call scores:
the state-level cross-party ideological gap (column 4, $p = 0.522$) and the national
ideological gap (column 5, $\beta = -0.009$, $p = 0.050$). The negative coefficient
on the national measure likely reflects a secular decline in market reactions to
auditor changes over the post-SOX period rather than a genuine polarization effect,
since the national gap rises monotonically and year fixed effects do not fully absorb
biennial congressional variation. Column (6) augments the baseline with state fixed
effects; the coefficient triples in magnitude but loses precision ($\beta = 0.012$,
$p = 0.144$), consistent with within-state variation in presidential competitiveness
being noisier than cross-state variation. Column (7) clusters standard errors at the
state level; the baseline coefficient is unchanged and remains significant ($\beta =
0.005$, $p = 0.030$), confirming that firm-level clustering is not driving the result.
Column (8) implements two-way clustering by firm and state following
\citet{cameron2011}, computing $V = V_{\text{firm}} + V_{\text{state}} -
V_{\text{firm} \times \text{state}}$; because firms are nested within states
(each firm's headquarters state is fixed), the intersection clusters equal the
firm clusters and the two-way estimator is equivalent to state-level clustering
in expectation.

\input{../Output/Tables/tab05_robustness}

\subsection{Supplementary Affective Polarization Test}

Table~\ref{tab:affective} reports the supplementary affective polarization test.
Column (1) repeats the baseline ideological result ($\beta = 0.005$, $p = 0.040$).
Column (2) adds the $AP \times \text{Exposure}$ interaction; the affective term is
near zero and statistically insignificant ($\beta = 0.000$, $p = 0.879$), while the
ideological coefficient is unchanged. Columns (3) and (4) add the ambiguity
interactions; the ideological coefficient attenuates toward zero once the
interaction term absorbs variation, but neither the affective main effect nor the
triple interaction reaches significance (all $p > 0.40$). Column (5) repeats the
full specification for abnormal volume: the ideological competitiveness coefficient
is larger and highly significant ($\beta = 0.130$, $p = 0.003$), while the affective
terms remain indistinguishable from zero. Taken together, Table~\ref{tab:affective}
provides no evidence that national affective polarization, interacted with state
partisan exposure, incrementally explains market reactions to auditor changes beyond
what ideological competitiveness captures. The results are consistent with state-level
ideological heterogeneity --- rather than the temporal rise in affective polarization
--- being the operative mechanism.

\input{../Output/Tables/tab06_affective}

% ── 7. Conclusion ─────────────────────────────────────────────────────────────

\section{Conclusion}
\label{sec:conclusion}

Political polarization affects how investors interpret auditor signals, with
implications for market efficiency and audit regulation. Identical disclosures
generate more extreme market reactions --- larger absolute returns and higher
trading volume --- in more polarized political environments. These effects are
stronger for ambiguous signals, consistent with a model in which polarization
widens disagreement in interpretation by amplifying the role of prior heterogeneity.

The findings suggest one limit to disclosure-based regulation in
credibility-sensitive settings: when investors differ in their assessments of
institutional trustworthiness, more disclosure does not eliminate disagreement. As
polarization has increased persistently over the past two decades, its effect
on audit signal interpretation may be growing. We study auditor changes as an
initial tractable setting, but the mechanism should operate wherever disclosure
interpretation depends on trust in regulated gatekeepers. Future research
should examine whether these patterns extend to critical audit matters, going
concern opinions, and internal control reports. A further promising direction
is to distinguish the roles of ideological and affective polarization ---
our proxies primarily capture ideological distance, but the recent political
science literature suggests that partisan distrust and identity-based animus
\citep{mason2015, druckmanlevy2021} may be independently important for how
investors assess the credibility of institution-dependent disclosures.

\bibliographystyle{apalike}
\bibliography{references}

% ── Appendix ──────────────────────────────────────────────────────────────────

\appendix

\section{Proof of Proposition~\ref{prop:dispersion} and Additional Theoretical Results}
\label{app:proofs}

\noindent\textbf{Setup.} Let there be two investor groups, $D$ and $R$, with
population shares $s_D, s_R > 0$ and $s_D + s_R = 1$. Group $g$ holds prior
$\pi_g \in (0,1)$. The mean prior is $\bar{\pi} = s_D \pi_D + s_R \pi_R$.
After observing signal $s=1$, investor $i$ in group $g$ holds posterior
$\phi(\pi_g)$, where the Bayesian updating function is
\[
    \phi(\pi) = \frac{p_H \pi}{p_H \pi + p_L(1-\pi)},
    \qquad p_H > p_L > 0.
\]
The function $\phi$ is strictly increasing and continuous on $(0,1)$.

\medskip
\noindent\textbf{Step 1: Variance formula.}
The posterior beliefs across groups follow a two-point distribution placing
mass $s_D$ at $\phi(\pi_D)$ and mass $s_R$ at $\phi(\pi_R)$. The population
mean posterior is $\bar{\phi} = s_D \phi(\pi_D) + s_R \phi(\pi_R)$. A
standard calculation gives:
\begin{align*}
    \mathrm{Var}_i[\phi(\pi_i)]
    &= s_D\bigl(\phi(\pi_D) - \bar\phi\bigr)^2
     + s_R\bigl(\phi(\pi_R) - \bar\phi\bigr)^2 \\
    &= s_D (s_R)^2 \bigl(\phi(\pi_D) - \phi(\pi_R)\bigr)^2
     + s_R (s_D)^2 \bigl(\phi(\pi_D) - \phi(\pi_R)\bigr)^2 \\
    &= s_D s_R (s_D + s_R) \bigl(\phi(\pi_D) - \phi(\pi_R)\bigr)^2 \\
    &= s_D s_R \bigl(\phi(\pi_D) - \phi(\pi_R)\bigr)^2,
\end{align*}
where the second equality uses $\phi(\pi_D) - \bar\phi = s_R(\phi(\pi_D) -
\phi(\pi_R))$ and $\phi(\pi_R) - \bar\phi = -s_D(\phi(\pi_D) - \phi(\pi_R))$,
and the last equality uses $s_D + s_R = 1$. This establishes the variance
formula in Proposition~\ref{prop:dispersion}.

\medskip
\noindent\textbf{Step 2: Monotonicity in $|\pi_D - \pi_R|$.}
Without loss of generality, assume $\pi_D > \pi_R$. A mean-preserving
spread that increases $|\pi_D - \pi_R|$ by $\epsilon > 0$ while holding
$\bar\pi$ fixed requires:
\[
    \pi_D' = \pi_D + s_R \epsilon, \qquad
    \pi_R' = \pi_R - s_D \epsilon,
\]
so that $s_D \pi_D' + s_R \pi_R' = s_D \pi_D + s_R \pi_R = \bar\pi$.

Since $\phi$ is strictly increasing:
\[
    \phi(\pi_D') = \phi(\pi_D + s_R\epsilon) > \phi(\pi_D),
    \qquad
    \phi(\pi_R') = \phi(\pi_R - s_D\epsilon) < \phi(\pi_R).
\]
Therefore:
\[
    \phi(\pi_D') - \phi(\pi_R')
    > \phi(\pi_D) - \phi(\pi_R) > 0.
\]
Since $s_D s_R > 0$ is unchanged, we conclude:
\[
    \mathrm{Var}_i[\phi(\pi_i')]
    = s_D s_R \bigl(\phi(\pi_D') - \phi(\pi_R')\bigr)^2
    > s_D s_R \bigl(\phi(\pi_D) - \phi(\pi_R)\bigr)^2
    = \mathrm{Var}_i[\phi(\pi_i)]. \qquad \square
\]

\bigskip

\noindent\textbf{Propositions A1--A3: Additional Channels.}

The following three propositions establish that the same posterior-dispersion
comparative static holds when polarization operates through institutional
trust and perceived signal precision, and nests all three channels in a
single latent-index representation.

\medskip
\noindent Let $\sigma(x) \equiv e^x/(1+e^x)$ denote the logistic map, with
$\sigma'(x) = \sigma(x)(1-\sigma(x)) > 0$ for all $x \in \mathbb{R}$.
For any prior $\pi \in (0,1)$, define prior log-odds $a(\pi) \equiv
\log[\pi/(1-\pi)]$ and objective log-likelihood ratio $\ell \equiv
\log(p_H/p_L) > 0$.

\begin{proposition}[Institutional trust channel]
\label{prop:trust}
Fix a common prior $\pi \in (0,1)$ and a favorable signal with
log-likelihood ratio $\ell > 0$. Suppose investor $i$ discounts the signal by
trust parameter $\tau_i \in [0,1]$, so that posterior beliefs are
$m(\tau_i) = \sigma(a(\pi) + \tau_i \ell)$. For two groups $D, R$ with trust
levels $\tau_D, \tau_R$:
\[
    \mathrm{Var}(m(\tau_g)) = s_D s_R \bigl[\sigma(a(\pi)+\tau_D\ell)
    - \sigma(a(\pi)+\tau_R\ell)\bigr]^2.
\]
Any increase in $|\tau_D - \tau_R|$ strictly increases $\mathrm{Var}(m(\tau_g))$.
\end{proposition}

\begin{proof}
Since $dm/d\tau = \sigma'(a(\pi)+\tau\ell)\cdot\ell > 0$, $m(\tau)$ is
strictly increasing. The variance formula follows from the two-point
distribution, as in Step 1 above. Strict monotonicity of $m$ implies
$|m(\tau_D)-m(\tau_R)|$ is strictly increasing in $|\tau_D-\tau_R|$,
which proves the claim. $\square$
\end{proof}

\begin{proposition}[Perceived precision channel]
\label{prop:precision}
Under the same setup, suppose investors share a common prior and common
trust but differ in perceived signal precision, scaling their effective
log-likelihood ratio by $\kappa_i > 0$: posterior beliefs are
$q(\kappa_i) = \sigma(a(\pi) + \kappa_i \ell)$. For two groups $D, R$:
\[
    \mathrm{Var}(q(\kappa_g)) = s_D s_R \bigl[\sigma(a(\pi)+\kappa_D\ell)
    - \sigma(a(\pi)+\kappa_R\ell)\bigr]^2.
\]
Any increase in $|\kappa_D - \kappa_R|$ strictly increases $\mathrm{Var}(q(\kappa_g))$.
\end{proposition}

\begin{proof}
Identical argument to Proposition~\ref{prop:trust} with $\kappa$ replacing
$\tau$. $\square$
\end{proof}

\begin{proposition}[Unified channel representation]
\label{prop:unified}
Let each investor have posterior $\mu_g = \sigma(X_g)$, where the latent
log-odds index $X_g = a(\pi_g) + \tau_g \kappa_g \ell$ may differ across
groups through any combination of heterogeneous priors $\pi_g$, trust $\tau_g$,
or perceived precision $\kappa_g$. Then:
\[
    \mathrm{Var}(\mu_g) = s_D s_R \bigl[\sigma(X_D) - \sigma(X_R)\bigr]^2,
\]
and any increase in $|X_D - X_R|$ strictly increases $\mathrm{Var}(\mu_g)$.
In particular, if polarization widens the cross-group gap in any one of the
three underlying components---prior log-odds, trust, or perceived
precision---posterior variance and disagreement-sensitive trading outcomes
strictly increase.
\end{proposition}

\begin{proof}
The variance formula is the two-point distribution result. Strict
monotonicity of $\sigma$ implies $|\sigma(X_D)-\sigma(X_R)|$ is strictly
increasing in $|X_D-X_R|$. Any widening of the gap in a single component
widens $|X_D-X_R|$ and therefore widens $|\sigma(X_D)-\sigma(X_R)|$.
$\square$
\end{proof}

\end{document}
